/// [Quiz 2025] Divide the number ///

#include <stdio.h>
 
int main() {
    int a, b;

    scanf("%d %d", &a, &b);

    printf("%.2lf", (double)a / (double)b);
 
    return 0;
}

///เขียนโค้ดแทบเหมือนกัน 100% ต่างกันแค่ 3 → 3.0 และ 18 → 18.0
///buggy:คอมไพเลอร์มองว่านิพจน์ทั้งหมดเป็น int → คำนวณ 39/18 แบบตัดเศษทิ้งตั้งแต่ต้น ได้ 2 แล้วค่อยแปลงเป็น double เป็น 2.0
///fixed:คอมไพเลอร์มองว่านิพจน์เป็น double เพราะมี .0 ปนอยู่ → คำนวณด้วย floating point ตั้งแต่ต้น ได้ 2.1666... ตรงตามค่าจริง